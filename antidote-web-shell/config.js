const config = {
    antidote: [
        {
            host: 'localhost',
            port: 8087
        },
        {
            host: 'localhost',
            port: 8088
        }
    ]
};

module.exports = config;

